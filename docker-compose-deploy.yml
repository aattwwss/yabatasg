services:
  yabatasg:
    container_name: yabatasg
    image: ghcr.io/aattwwss/yabatasg:latest
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ./data:/data
