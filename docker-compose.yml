services:
  yabatasg:
    container_name: yabatasg
    build:
      context: .
      args:
        UID: ${UID:-1000}
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ./data:/data
